## 文本输入

### 处理方式
- **纯 ASCII 文本**（如 `"hello123"`）→ scrcpy 快速注入
- **包含中文的文本** (如 `你好，ShuaiB😄`) -> 使用粘贴板 同步

### 工作流程

1. **设置剪贴板**：使用 ADB 命令将文本设置到远程设备的剪贴板
   - 使用 Base64 编码避免特殊字符问题
   - 兼容不同 Android 版本的剪贴板 API
   
2. **触发粘贴**：发送 `KEYCODE_PASTE (279)` 按键事件
   - 模拟用户按下粘贴键
   - 将剪贴板内容粘贴到当前输入框